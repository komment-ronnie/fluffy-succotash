/**
 * @description Logs the value `0` to the console when invoked. It is a simple arrow
 * function that does not accept any arguments and returns no value.
 */
const a = () => {

  
  console.log(0);

}
