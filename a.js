/**
 * @description Defines an immediately invoked anonymous function (IIFE) that logs
 * '0' to the console when called. The function is executed as soon as it's defined,
 * making it a self-contained block of code with no explicit return value.
 */
const a = () => {

  
  console.log(0);

}
